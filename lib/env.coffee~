class Environment
  constructor: (params, args, outer) ->
    for p, i in params
      @[p] = args[i]
    @outer = outer

  find: (v) ->
    if @[v]?
      @
    else
      @.outer.find v

exports.Environment = Environment
